[build]
builder = "nixpacks"

[deploy]
releaseCommand = "python -m hasl_calendar.migrate"
startCommand = "gunicorn 'hasl_calendar.app:app' --bind 0.0.0.0:$PORT --workers 2"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
