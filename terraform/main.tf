# Railway resources temporarily removed so removed.tf can clear stale state.
# After applying:
#   1. git checkout -- terraform/main.tf
#   2. delete removed.tf
#   3. terraform apply to recreate everything
